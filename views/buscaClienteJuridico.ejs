<!DOCTYPE html>
	<html lang="pt-br">
		<head>
			<meta charset="utf-8"/>
			<title>RegistroClientes</title>
			<script src="jquery/jquery.js" type="text/javascript"></script>
			<link rel="stylesheet" type="text/css" href="css/buscaGeral.css">

			<script type="text/javascript">
				$(function(){
					/*Selecionar todos os tr's.
					Selecionar todos os botões edit
					Adicionar evento de clique em cada um
					Adicionar uma div caso clicado
					*/
					/*$('.id').mouseover(function(){
						var valor = $(this).attr('id');
						var input = $(this);
						$(this).val('Editar');

						$(this).mouseout(function(){
							$(this).val(valor);

							
						})
					})
					
    			})*/
			</script>
		</head>
	<body>
	<form autocomplete="off" action='/red' method='get'>
		<h1><input type="submit" class="HOME" value="Início" name="HOME"></input></h1>
	</form>
		<h2>Registros de Clientes Jurídicos</h2>

		<table>
			
			<tr>
				<th>ID</th>
				<th>Cliente</th>
				<th>CNPJ</th>
				<th>Telefone(1)</th>
				<th>Telefone(2)</th>
				<th>Telefone(3)</th>
				<th>Endereço</th>
				
			</tr>
			<% if(cliente){ %>
			<% for(var i = 0; i < cliente.length; i++){ %>
				
				<tr>
					
					<td>
						<form action = '/editJuridico' method = 'post'>
							<input class = 'id' id='<%=cliente[i].id%>' type='submit' name='id' value='<%= cliente[i].id %>'>
						</form>
					</td>
					<td><%= cliente[i].nome %></td>
					<td><%= cliente[i].cnpj%></td>
					<td><%= cliente[i].telefone%></td>
					<td><%= cliente[i].telefone2%></td>
					<td><%= cliente[i].telefone3%></td>
					<td><%= cliente[i].endereco%></td>

				</tr>
			<% } } %>

		</table>

	</body>
</html>
				